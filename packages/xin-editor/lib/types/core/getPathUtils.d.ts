import { Editor, Path as SlatePath } from 'slate';
import { CustomElementType } from '../types';
export declare function getParentPathByType(editor: Editor, path: SlatePath, type: CustomElementType): number[] | null;
